schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/sqlc-returning-embedded-table-models"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "kh27079eab705120f8a563cc5cb4cd97"
task_id = "sqlc-returning-embedded-table-models"
display_title = "Preserve table models in old-row returns"
display_description = "Generate the original table model when rewritten mutation queries return embedded old-row values."
original_title = "改写返回旧行时，嵌入整表必须还能认出原来的模型"
category = "feature_request"
language = "go"
repository_url = "https://github.com/sqlc-dev/sqlc"
base_commit_hash = "a461f8aa74be5304b7498026126398d2e9ba5313"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary a461f8aa74be5304b7498026126398d2e9ba5313 HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:kh27079eab705120f8a563cc5cb4cd97-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
